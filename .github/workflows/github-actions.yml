name: Build resume html
on: 
  pull_request:
  push: 
    branches: 
      - main

jobs:
  build_resume:
    runs-on: ubuntu-20.04
    container: yupiter/my-resume
    steps:
      - run: ls
      - run: make build_html
